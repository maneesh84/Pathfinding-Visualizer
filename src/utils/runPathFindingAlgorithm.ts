import type { GridType, TileType } from "./Types"


export const runPathFindingAlgorithm=({
  algorithm,
  grid,
  startTile,
  endTile,
}:{
  algorithm:Algorithm,
  grid:GridType,
  startTile:TileType,
  endTile:TileType,
})=>{

  switch(algorithm){
    case "bfs":
      //return;
    default:
      return//;
  }
}